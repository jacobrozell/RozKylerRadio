.DS_Store
Thumbs.db
*.tmp

# GitHub: plain Git blocks files >100 MB; Pages published site max 1 GB.
# Ship lossy MP3 in git; keep huge lossless out (still on disk locally).
/Renders/**/*.wav
/Renders/**/*.flac
/Renders/**/_robocopy_log.txt
